import 'package:flutter/material.dart';

class MatchStatisticsWidget extends StatelessWidget {
  final String matchId;
  const MatchStatisticsWidget({Key? key, required this.matchId})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
