class rol_assignment_model{
  String id;
  int avatar_id;
  String full_name;
  List<String> my_roles;
  List<String> all_roles;

  rol_assignment_model(this.id, this.avatar_id, this.full_name, this.my_roles, this.all_roles);
}